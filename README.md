# Schema Foundation Package
This package contains the locked PR spec for the first real implementation step after repo planning lock.

Add this to the repo, then execute the schema foundation PR against:
- implementation matrix
- countdown map
- acceptance gates
- governing canon
